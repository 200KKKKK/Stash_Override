name: CustomRules
desc: 添加自定义策略组和分流规则

override:
  proxy-groups:
    - name: 💡 Claude
      type: select
      proxies:
        - DIRECT

  rules:
    - DOMAIN-SUFFIX,claude.ai,💡 Claude